input {
  tcp {
    port => 9999
    codec => line
    ssl => false
  }
}
 
filter {
  json {
    source => "message"
    recursive => "true"
  }
}
 
output {
  elasticsearch {
	hosts => "localhost:9200"
	}
}
